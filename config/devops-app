server {
       listen 80;
       listen [::]:80;

       server_name example.ubuntu.com;

       root /var/www/devops-app;
       index index.php;

       location / {
               try_files $uri $uri/ =404;
       }
}